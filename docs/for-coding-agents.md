# UI development

For changes to the web app or to storybook use playwright to validate your changes or investigate issues.

The web app usually runs on http://localhost:5173/, storybook on http://localhost:6006/. Ask the user to start the respective environment if it's unavailable.